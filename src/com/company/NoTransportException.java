package com.company;

public class NoTransportException extends RuntimeException {
    // this is a specific type of RuntimeException, so I don't actually need anything in the body right now
    
    // DON'T need to use a try/catch or throws

    // ideas: properties to indicate severity or something
}
